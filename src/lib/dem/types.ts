/**
 * Common type definitions for DEM processing.
 *
 * Re-exports standard GeoJSON types for convenience.
 */

// Re-export GeoJSON types consumers might need
export type {
  Position,
  Feature,
  LineString,
  Polygon,
  Geometry,
} from 'geojson';
